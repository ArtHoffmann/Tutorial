export interface UserModel {
    user_id: string;
    firstName: string;
    lastName: string;
}
export class UserModel {
    // tslint:disable-next-line:variable-name
    user_id: string;
    firstName: string;
    lastName: string;
}
